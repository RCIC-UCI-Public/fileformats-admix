# Builds the module for udunits.
!include udunits.yaml
!include /opt/rcic/include/rcic-module.yaml
---
- package: "{{name}} module"
  category: TOOLS
  module:
    setenv:
      - UDUNITS_HOME {{root}}
