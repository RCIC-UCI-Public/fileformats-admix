# Builds the module for udunits.
!include udunits.yaml
!include rcic-module.yaml
---
- package: "{{name}} module"
  category: TOOLS
  release: 2
  module:
    <<: *ModuleCompiler
    setenv:
      - UDUNITS_HOME {{root}}
