# Builds the module for udunits.
!include udunits.yaml
!include rcic-module.yaml
---
- package: "{{name}} module"
  category: TOOLS
  release: 2
  module:
    <<: *ModuleCompiler
    setenv:
      - *Setenv
      - UDUNITS_HOME {{root}}
