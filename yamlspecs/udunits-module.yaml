# Builds the module for udunits.
!include udunits.yaml
!include rcic-module.yaml
---
- package: "{{name}} module"
  category: TOOLS
  module:
    <<: *ModuleCompiler
    setenv:
      - UDUNITS_HOME {{root}}
