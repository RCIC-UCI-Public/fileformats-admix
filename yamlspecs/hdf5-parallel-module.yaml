# Builds the module for parallel version of hdf5. 
!include hdf5-parallel.yaml
!include /opt/rcic/include/rcic-module.yaml
---
- package: hdf5 parallel module 
  pkgname: "{{ name }}_{{version}}_{{compiler}}_{{compiler_ver}}_{{mpi}}_{{mpi_ver}}-module"
  category: TOOLS
  module:
    name: "{{ version }}"
    path: "{{ pkg_defaults.module.RCICmpath }}/{{category}}/{{name}}/{{compiler}}/{{compiler_ver}}/{{mpi}}/{{mpi_ver}}"
    prepend_path:
      - "{{ pkg_defaults.module.prepend_path }}"
      - -d " " LDFLAGS "-L{{root}}/lib"
      - -d " " CPPFLAGS "-I{{root}}/include"
    setenv:
      - HDF5_HOME {{ root }}
    prereq:
      - java/{{versions.java}}
      - "{{ compiler }}/{{compiler_ver}}" 
      - "{{ mpi }}/{{mpi_ver}}" 
  requires:
    - "{{ name }}_{{ version }}_{{compiler}}_{{compiler_ver}}_{{mpi}}_{{mpi_ver}}"
    - rcic-module-support
