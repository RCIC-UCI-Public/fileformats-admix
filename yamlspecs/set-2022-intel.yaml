!include packages.yaml
---
versions: versions-2022-intel.yaml
bootstrap:
  - !eval "'hdf5' if {{site.os_release}} <= 9 else 'hdf5-intel-patched'"
  - hdf5-module
  - "{{bootstrap_hdf5_para}}"
  - "{{bootstrap1}}"
build:
  - ioapi
  - ioapi-module
